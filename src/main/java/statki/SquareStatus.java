package statki;

public enum SquareStatus {
    OCEAN,
    SHIP,
    HIT,
    MISS,
    SUNK;

    /**
     * TODO:
     *  - unicode character that can be used for printing it out,
     *    this char is returned by a SquareStatus.GetCharacter() method
     */
}
