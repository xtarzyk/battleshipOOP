package statki;

public class Board {
    /**
     * TODO:
     *  - Square[][] ocean field (squares that the board consists of)
     *  - isPlacement() (verifies if placement of ships is possible),
     */
}
